<%= form_for(@question) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, placeholder: "Title your question" %>
  <%= f.label :content %>
  <%= f.text_area :content, placeholder: "Ask a question!" %>
  <%= f.submit "Ask" %>
<% end %>
