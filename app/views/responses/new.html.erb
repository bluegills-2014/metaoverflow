<% if @answer %>
  <%= content_for :title, "Responding to #{@question.title}" %>
<% else %>
  <%= content_for :title, "Responding to #{@question.title}" %>
<% end %>
<%= @question.title %>
<%= render 'form' %>
